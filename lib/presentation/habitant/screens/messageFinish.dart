import 'package:flutter/material.dart';

class MessageFinish extends StatelessWidget {
  const MessageFinish({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MessageFinish'),
    );
  }
}
